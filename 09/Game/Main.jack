/**
 * The Main class initializes a new Gravity game and starts it.
 */
class Main {
	/** Initializes a new game and starts it. */
	function void main() {
		var GravityGame game;
		let game = GravityGame.new();
		do game.run();
		do game.dispose();
		return;
	}
}
